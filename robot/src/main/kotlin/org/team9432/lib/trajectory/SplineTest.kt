package org.team9432.lib.trajectory

import edu.wpi.first.math.geometry.Pose2d
import org.littletonrobotics.junction.Logger

class SplineTest(private vararg val poses: Pose2d) {
    init {

    }

    var totalDistance = 0.0

//    fun display(resolution: Int) {
//        val points = mutableListOf<Pose2d>()
//        for (i in 0..resolution) {
//            points.add(calculatePoint(i * (totalDistance / resolution)))
//        }
//    }

//    fun calculatePoint(distance: Double): Pose2d {
//
//    }
}